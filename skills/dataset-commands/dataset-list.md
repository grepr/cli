# grepr dataset:list

## Command Help
!`grepr dataset:list --help`

## When to Use
List all datasets (data lake tables) where processed logs are stored. Use this to see available data sources for querying from or writing to.

## Examples

```bash
# List all datasets
grepr dataset:list

# List with JSON output
grepr dataset:list -f json
```
